#menu-title
  %h1#content-title 
    #{@project.name} &raquo; Edit #{@data_collection.name} Data
#menu-actions
  = link_to("Back", yogo_project_collection_items_path(@project, @data_collection), :class => 'icon icon-back')
  |
  = link_to("Projects", yogo_projects_path, :class => 'icon icon-dashboard')
  |
  = link_to("Project Dashboard", yogo_project_path(@project), :class => 'icon icon-projects')
