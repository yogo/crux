#menu-title
  %h1#content-title 
    #{@project.name} &raquo; Import Spreadsheet Step #{@import_step} of #{@total_steps}
#menu-actions
  = link_to("Back to Experiment Dashboard", yogo_project_path(@project), :class => 'icon icon-back')
